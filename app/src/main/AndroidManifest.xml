<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mentalhealth">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/tej_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity" android:windowSoftInputMode="adjustPan|stateUnchanged"
            android:theme="@style/SplashTheme" >
        </activity>

        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar"
            android:windowSoftInputMode="adjustPan|stateUnchanged"
            android:name=".RegisterActivity" />
        <activity android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar"
            android:name=".Patient_feed" />

    </application>


<!--android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar"-->
</manifest>